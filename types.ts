export type HabitTypes = {
  id: number | string;
  habit: string;
  freq: string;
  completed: boolean;
  noLeft: number | string;
  period: string;
};
export type TodoTypes = {
  id: number | string;
  todo: string;
  completed: boolean;
};
